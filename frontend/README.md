# Spenza Webhook Frontend

Basic React dashboard for the webhook backend.

## Setup

```bash
npm install
npm run dev
```

Open `http://localhost:5173`.

The frontend expects the backend to run on `http://localhost:4000`.
